"\"use client"

import type { LogoSettings } from "@/types"

interface LogoPreviewProps {
  settings: LogoSettings
}

const LogoPreviewComponent = ({ settings }: LogoPreviewProps) => {
  return null
}

export default LogoPreviewComponent

